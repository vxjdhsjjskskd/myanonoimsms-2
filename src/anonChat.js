// src/anonChat.js - Заглушка, так как логика перенесена в app.js

// Этот файл не используется напрямую в текущей логике app.js,
// но оставлен для совместимости или будущих расширений.

module.exports = {
    sendAnonymousMessage: async () => console.log("sendAnonymousMessage (заглушка)"),
    sendAnonymousReply: async () => console.log("sendAnonymousReply (заглушка)")
};
